public class TweetParser {

    private string DataPath = String.Empty;

    public TweetParser(string dataPath) {
        DataPath = dataPath;
    }

    public List<State> GetStatesByTopicFile(string topicFile) {
        List<State> states = new List<State>();

        

        return states;
    }
}
